version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./files:/app/files
    environment:
      - PYTHONPATH=/app
    command: uvicorn src.app:app --host 0.0.0.0 --port 8000 --reload
